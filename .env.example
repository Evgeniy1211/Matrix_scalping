# Copy this file to .env and fill in the values.

# Application
# Optional. Port for the Express server. Defaults to 5000.
PORT=5000

# Environment
# Optional. NODE_ENV is set by npm scripts automatically, override only if needed.
#NODE_ENV=development

# Database
# Required by Drizzle (drizzle.config.ts). Postgres connection string.
# For local Postgres, example:
# DATABASE_URL=postgres://user:password@localhost:5432/matrix_scalping
# For Neon (serverless Postgres), example:
# DATABASE_URL=postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=

# Replit (optional)
# REPL_ID is used to enable cartographer plugin in Vite when running on Replit.
# Leave empty for local dev.
#REPL_ID=