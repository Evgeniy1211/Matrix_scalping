/**
 * DEPRECATED: This file is intentionally left as a stub to avoid duplication.
 * Unified schemas live in `backend/schemas/schema.ts` and are imported via alias `@shared/schema`.
 *
 * Please update any imports that still reference `backend/knowledge-base/schema.ts` to `@shared/schema`.
 */
export {};
